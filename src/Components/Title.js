import React from 'react';

const Title = () => {
    return (
        <div className= ' w-3/4  mx-auto mt-[-30px] md:mt-[-100px] p-4 md:p-10 bg-white    '>
            <h4 className='text-center'> PRODUCTS</h4>
            <h1 className='text-center text-2xl p-2'>POWER TOOLS</h1>
            <p className='text-xs md:text-lg'>Our power tools are made to live on the jobsite. That’s where they’re tested. And if there’s a problem, it’s where we solve it. The result is tools that we can stand behind.</p>
        </div>
    );
};

export default Title;